# Optional. Leave unset for normal local dev: the dev server proxies /api and
# /actuator to the backend (vite.config.ts), so the app's calls are same-origin
# and need no base URL. Set this only to call a backend directly, bypassing the
# proxy - e.g. a backend on another host or port.
VITE_API_BASE_URL=http://localhost:8080
